import pandas as pd


def check_results_are_sorted(results: pd.DataFrame):
    pass
    # TODO
    # if not pd.to_datetime(results["Time"]).is_monotonic_increasing:
    #     print("Results are not sorted")
    #     print(results)
